#include "main.h"

/**
* sub - gets the difference of two numbers
* @a: first integer
* @b: second integer
* Return: difference of the two numbers
*/
int sub(int a, int b)
{
	int diff;

	diff = a - b;
	return (diff);
}
