#include "main.h"

/**
* mul - gets the product of two numbers
* @a: first integer
* @b: second integer
* Return: product of the two numbers
*/
int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
