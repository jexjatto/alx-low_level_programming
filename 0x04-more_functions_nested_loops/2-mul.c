#include "main.h"

/**
* mul - multiplies two integers
* @a: an integer
* @b: second integer
*
* Description: the two integers will multiply each other
*
* Return: returns an int
*/
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
